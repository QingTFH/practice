利用数组元素的特性(例如nums[i] > 0)，或者数组本身的特性(例如升序数组),
在遍历到nums[i]时, 可以直接考虑后续需要和他配对的情况, 并预先存入map中以便于实现O(1)查找

例如, 要求找到nums[i] + nums[j] == 0的配对个数, 在遍历到nums[i]时,
可以考虑可能存在的nums[j] = -nums[i], 因此预先存入map: -nums[i] -> map.get()+1
当确实遍历到nums[j]时, 直接map.get(nums[j])即可获得对应个数,
实现O(1)时间查找了[0, j]的元素内容