以下都以大顶堆为例
堆是特殊的完全二叉树, 父节点大于等于任意子节点;
完全二叉树: 除了最后一层外, 其余层数都是满的; 最后一层必须从左到右排列; 因此方便用数组来模拟
父节点i, 子节点2*i+1 和 2*i+2 (i从0开始)

常用方法:
- 上浮: 给叶子节点找位置, 用于插入新元素
- 下沉: 给根节点找位置, 用于组合两个堆、删除根节点
- 原地堆化: 从倒数第二层开始逆序遍历每个节点, 对每个节点进行下沉以组合左右两个堆
    对于size = n的完全二叉树, 叶子节点的个数是 Math.ceil(n / 2) (向上取整的意思)

跳过了 5.5 反悔堆、 5.6 懒删除堆、 5.7 对顶堆