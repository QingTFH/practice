# AGENTS.md

面向 AI 代理（以及协作者）的仓库约定，帮助快速理解本仓库并保持目录与命名一致。

## 仓库定位

本仓库是个人 LeetCode 刷题记录区，按题单分类存放解题代码。分类题目全部来源于**灵茶山艾府**的「[分享｜如何科学刷题？](https://leetcode.cn/discuss/post/3141566/ru-he-ke-xue-shua-ti-by-endlesscheng-q3yd/)」题单。

## 目录结构

```
src/
├── random/          # 每日一题
├── 常用数据结构/    # 差分 / 堆 / 队列 / 枚举技巧 / 前缀和 / 栈
│   └── 枚举技巧/    #   枚举中间 / 枚举右维护左
├── 二分查找/
├── 单调栈/
├── 滑窗/            # 不定长滑窗 / 定长滑窗
└── 网格图/
```

`src/out/` 为编译输出目录，由 `.gitignore` 忽略，不入库。

## 命名与放置规则

- 题目解答：`a<题号>.java`，`<题号>` 为 LeetCode 题号。解答放入对应题单分类目录；每日一题放入 `src/random/`。
- 模板代码：放入对应分类的 `_banzi.java`（如 `src/二分查找/_banzi.java`）。
- 知识点笔记：放入对应分类的 `说明` 文件（无扩展名，如 `src/常用数据结构/堆/说明`）。
- 目录/文件命名使用中文，与现有结构保持一致，不要新建英文别名目录。

## 提交纪律

- 不提交 IDE 配置文件：`.idea/`、`*.iml`。
- 不提交编译产物：`src/out/`。
- 提交前确认 `git status` 中仅包含预期的源码与文档变更。
